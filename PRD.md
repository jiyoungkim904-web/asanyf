# PRD 최종본 — 영팜마켓AI

> **제출 버전 / 2026-09-09**
> 이 문서 하나만 읽어도 다른 사람이 "영팜마켓AI가 어떤 서비스인지" 설명할 수 있도록 작성했습니다.
> 초기 기획본(`PRD_v1.md`)에서 실제 구현·의사결정에 맞춰 전면 개정한 최종본입니다. 변경 이력은 §12에 정리했습니다.

---

## ① 한 줄 정의

**영팜마켓AI는 전국 농가가 농산물 데이터를 입력하면, 생성형 AI가 판매용 상세페이지와 SNS 숏폼 영상을 만들어 주고, 그 콘텐츠에서 곧바로 온라인 판매·공동구매까지 연결하는 "농업 특화 생성형 AI 커머스 플랫폼"이다.**

- 데이터: 농가가 입력하는 농산물 정보(품종·재배방식·수확시기·당도·인증·스토리·가격·사진 등)
- AI: 그 데이터를 판매 상세페이지 초안 → 숏폼 영상 스크립트(씬·나레이션·자막) 순으로 변환
- 커머스: 완성된 콘텐츠에 붙는 스토어 판매·공동구매·정기구독·B2B 소싱
- 브랜드 콘텐츠: 진행자 "서영"이 산지를 직접 찾아가는 오리지널 시리즈 **〈서영왔서영〉**

---

## ② 문제 — 지금 무엇이 불편한가

| 대상 | 불편 |
|---|---|
| **지역 농가** | SNS·상세페이지용 홍보 콘텐츠를 만들 시간·인력·촬영/편집 노하우가 없다. 외주는 건당 비용이 크고, 제철마다 새로 만들기 어렵다. |
| **농가 데이터** | 품종·재배방식·수확시기·당도·인증 같은 정보가 머릿속·수첩·문자에 흩어져 있어, 콘텐츠나 상세페이지에 바로 쓸 수 있는 "정리된 데이터"가 없다. |
| **소비자·셀러** | 산지 정보가 불투명하다. "누가, 어떻게 키운 것인지" 믿고 살 근거가 약하다. |
| **온라인 셀러** | 좋은 산지를 도매로 연결받기 어렵고, 소싱한 상품의 콘텐츠도 직접 만들어야 한다. |

핵심 가설: **"농산물 정보를 구조화된 데이터로 한 번 입력하면, 콘텐츠 제작–판매–신뢰 확보를 한 흐름에서 자동화할 수 있다."**

---

## ③ 사용자와 상황 (페르소나 3종)

### 1) 농가 (사장님) — 콘텐츠를 만들어 파는 사람
- 본인 농산물 정보를 직접 입력하고, AI가 만든 상세페이지·영상을 확인·수정한 뒤 받아서 SNS·판매에 활용한다.
- 스토어 판매·공동구매·정기구독을 열어 실제 매출을 만든다.
- **화면**: 데스크톱 웹(`/dashboard` 이하). 로그인 데모 계정 `sunfarm@example.com` / `test1234`.

### 2) 구매자 (소비자·셀러) — 콘텐츠를 보고 사는 사람
- 앱에서 산지 콘텐츠·숏폼·〈서영왔서영〉을 보고 농산물을 구매하거나 공동구매에 참여, 정기구독을 신청한다.
- 셀러라면 **소싱** 탭에서 산지 도매를 신청한다.
- **화면**: 모바일 앱 형태의 셸(`/shop` 이하, 하단 탭바). 데모 계정 `buyer@example.com` / `test1234`.

### 3) 운영자 (영팜마켓AI 팀) — 품질을 지키는 사람
- 여러 농가의 입력 데이터를 확인하고, AI 생성 결과(상세페이지·영상 스크립트)를 검수해 승인/반려한다.
- 커머스·매출(5개 수익모델)을 모니터링한다.
- 실제 완성형 영상은 **AI 영상 생성 스튜디오**에서 직접 만들어 상세페이지에 첨부한다.
- **화면**: `/admin` 이하. 데모 계정 `admin@youngfarm.ai` / `admin1234`.

---

## ④ 목표와 성공 기준

| 목표 | 측정 지표 |
|---|---|
| 농가가 스스로 농산물 데이터를 구조화해 입력한다 | 6단계 입력 폼 완료율, 선택 항목(당도·인증·가격대 등) 채움 비율 |
| 입력 데이터로 AI가 상세페이지·영상을 생성한다 | 농가당 월평균 상세페이지·영상 생성 건수, 재생성(다시 생성) 비율 |
| 콘텐츠가 실제 판매로 이어진다 | 콘텐츠→상품 상세 전환, 공동구매 목표 달성률, 스토어 GMV |
| 예치금(코인) 모델이 돈다 | 충전 전환율, 1인당 월 코인 사용량, 잔액 소진 후 재충전율 |
| 운영자 검수가 병목이 아니다 | 요청→검수완료 리드타임, 반려율 |

---

## ⑤ v1 범위 — 이번 버전에서 만든 것 (구현 완료)

### 농가
- 회원가입/로그인, 농가 프로필, 가입 시 체험 코인 `100코인` 지급
- **농산물 데이터 입력 폼 (6단계)**: 기본정보 · 재배정보 · 농가 이야기 · 판매·배송 · 사진 · 확인
  - 필드: 품목·품종·지역·재배방식·수확시기·이야기·사진(필수) + 등급/규격·포장 단위·재배 경력·인증(무농약/유기농/GAP/저탄소/지리적표시/HACCP)·당도·구체 수확시기·강점 한 줄·재배 시 신경 쓰는 점·가격대·배송 방법·보관법·선물 포장 여부(선택)
- **AI 상세페이지 생성·수정·확정** (`/products/:id/detail`)
  - AI가 배지·헤드라인·보조문구·셀링포인트 3~4개·다듬어진 스토리·상품정보 표·보관법·FAQ·마무리 문구를 생성
  - 농가가 문구를 직접 수정 가능, "다시 생성" 가능, 확정하면 스토어 상품 상세와 영상 제작의 재료가 됨
- **AI 숏폼 영상 요청** (`/content/request`)
  - 확정된 상세페이지를 토대로 스크립트(후킹 문구·씬별 화면/나레이션·자동 자막·SNS 게시글·해시태그) 생성
  - 각 씬에 농가가 올린 실제 사진을 배정, TTS 내레이션 보이스 자동 선택 → **템플릿 조립 방식**
  - 요청 시 `290코인` 차감(잔액 부족 시 제작 불가 → 충전 안내)
  - 자동 진행(약 18초): 분석 → 스크립트 → 제작 → 검수 대기
- **AI 영상 생성 스튜디오** (`/studio`) — 완전 생성형(실제 영상 파일)
  - 내 OpenRouter API 키로 실제 비디오 생성 모델(Veo·Sora·Seedance·Wan 등) 호출
  - 텍스트 프롬프트 + 시작/끝 프레임 이미지, 모델별 지원 길이·해상도·화면비·가격 표시, 예상 비용 추정
  - 생성 결과 브라우저 재생 + 다운로드, IndexedDB에 영구 보관("내가 만든 영상")
  - 저장한 영상을 특정 상품의 상세페이지에 첨부 → 스토어 상세·편집기의 "영상으로 보기"에 노출
- **콘텐츠 목록/상세** (`/contents`): 검수 완료 후 콘텐츠 패키지(스크립트+게시글) 다운로드
- **예치금(코인) 관리** (`/billing`): 잔액, 충전 팩, 부가서비스, 코인 입출금 내역
- **판매 관리** (`/store`): 스토어 상품 등록, 공동구매 개설, 정기구독자 관리

### 구매자 앱 (`/shop`, 하단 탭 홈·소싱·콘텐츠·공구·마이)
- 홈: 오늘의 산지 캐러셀, 퀵메뉴, 추천 농산물·공구·숏폼 레일, 최신 〈서영왔서영〉
- 소싱: B2B 산지 도매 카탈로그(지역·품목·가격·수확시기 필터), 문의·소싱 신청
- 콘텐츠(스튜디오): AI 콘텐츠 소개, 예치금 충전 팩, 실제 영상 생성 스튜디오 진입
- 공구: 공동구매 목록·상세(참여 인원·카운트다운·목표 kg·%·참여)
- 마이: 소싱 내역 · 정기구독 · 주문 내역
- 그 외: 검색, 상품 상세(확정 상세페이지+첨부 영상 노출), 장바구니·결제, 정기구독 신청, 〈서영왔서영〉 시리즈, 숏폼 피드

### 운영자 (`/admin`)
- 대시보드: 농가·구매자·검수 대기·공동구매·수익모델별 현황
- 농가 목록·상세: 입력 데이터·상세페이지·콘텐츠·판매·정기구독
- 콘텐츠 검수: AI 생성 결과(스크립트·씬·자막·보이스) 확인 → 승인(발행)/반려(메모)
- 커머스 현황·매출: 5개 수익모델 합산, 소싱/공동구매/정기구독 집계, 농가별 코인 잔액·원장
- 내 계정: 표시 이름·권한·세션·데모 데이터 초기화
- AI 영상 생성 스튜디오

### 공통
- 웹은 데스크톱 레이아웃, 구매자 앱은 모바일 셸 — **한 코드베이스에서 뷰포트로 자동 전환**(880px)
- 색상: 가을 톤(주황·흰색·브라운). 강조 문구 "농업 특화 생성형 AI 커머스 플랫폼"은 주황색

---

## ⑥ 범위 밖 — v1에서 만들지 않은 것 (이유)

| 제외 항목 | 이유 |
|---|---|
| 사람 검수 없는 완전 자동 발행 | 콘텐츠 품질이 아직 검증 전 → 운영자 승인 후 전달 |
| 농가 SNS 자동 연동·자동 게시 | 계정 연동·권한 문제 → v1은 다운로드까지, 게시는 농가가 직접 |
| 농가용 영상 세부 편집기 | 구현 난도 높음 → v2 검토. 대신 문구 수정·다시 생성·프리미엄 스튜디오 제공 |
| 실제 PG 결제 | 프로토타입은 목(mock) 결제. 코인 충전·상품 결제 모두 연출 |
| 실제 백엔드 서버/DB | 상태는 브라우저 `localStorage`(목 DB). API 레이어(`src/lib/api.ts`)만 실서버로 교체하면 됨 |
| 지자체·B2G 사업 | 사업 방향이 아님. 관련 기능·문구 넣지 않음 |
| OpenRouter 호출 프록시 | 브라우저에서 직접 호출(BYOK). 배포 시 얇은 프록시 필요할 수 있음 |

---

## ⑦ 화면과 흐름

### 농가 핵심 흐름
```
회원가입 (+체험 코인 100)
  → 농산물 등록 (6단계 폼)
  → AI 상세페이지 생성 → 문구 확인·수정 → 확정
  → 예치금 충전 (없으면 영상 제작 불가)
  → AI 숏폼 영상 요청 (290코인 차감) → 자동 진행 → 검수 대기
  → (운영자 승인) → 콘텐츠 상세에서 패키지 다운로드
  → [선택] AI 영상 생성 스튜디오에서 실제 영상 생성 → 상세페이지에 첨부
  → 판매 관리에서 스토어 상품·공동구매·정기구독 개설
```

### 운영자 흐름
```
로그인 → 대시보드 → 농가/콘텐츠 목록
  → 콘텐츠 검수 (스크립트·씬·자막·보이스 확인)
  → 승인(발행) 또는 반려(수정 메모)
  → 커머스·매출 모니터링
```

### 구매자 흐름
```
앱 진입 → 홈/숏폼/〈서영왔서영〉 탐색
  → 상품 상세(확정 상세페이지 + 첨부 영상)
  → 단건 구매 / 공동구매 참여 / 정기구독 신청
  → (셀러) 소싱 탭에서 산지 도매 신청
```

### 주요 라우트

| 영역 | 경로 |
|---|---|
| 공개 | `/` `/signup` `/login` `/pricing` `/studio/video` |
| 농가 | `/dashboard` `/products/new` `/products/:id` `/products/:id/detail` `/content/request` `/studio` `/contents` `/contents/:id` `/billing` `/store` |
| 구매자 앱 | `/shop` `/shop/search` `/shop/sourcing` `/shop/studio` `/shop/reels` `/shop/sanji` `/shop/product/:id` `/shop/groupbuy(/:id)` `/shop/subscribe` `/cart` `/shop/login` `/shop/signup` `/shop/mypage` |
| 운영자 | `/admin/login` `/admin` `/admin/farms/:id` `/admin/contents(/:id)` `/admin/commerce` `/admin/revenue` `/admin/account` `/admin/studio` |

---

## ⑧ 데이터와 AI

### 데이터 모델 (목 DB, `localStorage` 네임스페이스 `youngfarm.v9`)

| 컬렉션 | 내용 |
|---|---|
| `farms` | 농가 정보 |
| `products` (farm_products) | 농산물 데이터 — 품종·재배방식·수확시기·당도·인증·스토리·가격·사진 + 도매(wholesale) 정보 |
| `detailPages` | AI 상세페이지 (초안/확정), 첨부 영상 목록(`videos[]`) |
| `requests` / `contents` | 콘텐츠 생성 요청 / 생성된 영상·스크립트·상태 |
| `coinWallets` / `coinTxns` | 농가 코인 잔액 / 충전·보너스·사용·환불 내역 |
| `orders` | 건별 부가서비스(촬영·편집) 주문 (코인 차감) |
| `listings` / `shopOrders` | 스토어 상품 / 구매 주문 (플랫폼 수수료) |
| `groupBuys` | 공동구매 |
| `produceSubs` | 소비자 농산물 정기구독 (B2C) |
| `farmPosts` / `sanjiEpisodes` | 농장 소식 / 〈서영왔서영〉 산지 방문 시리즈 |
| `sourcingRequests` | B2B 소싱 신청 |

- **권한 분리**: 농가/구매자 조회 함수는 모두 소유자 id를 받아 본인 데이터만 반환. 다른 농가 상품 직접 조회 차단.
- **연출 장치**: `tick()`이 읽을 때마다 경과 시간 기준으로 콘텐츠 상태를 자동 전진(요청→분석→스크립트→제작→검수). 실제 비동기 처리 없이 파이프라인을 시연.

### AI 레이어

**(1) 데모 파이프라인 — 템플릿 조립** (`src/lib/ai.ts`의 `AIProvider` 인터페이스, 현재 `MockAIProvider`)
- `generateDetailPage(product)` → 농산물 데이터 → 상세페이지 초안(배지·헤드라인·셀링포인트·스펙표·스토리·보관법·FAQ·마무리)
- `generateScript(product, length, detail?)` → 확정 상세페이지를 재료로 숏폼 스크립트(후킹·씬별 화면/나레이션·씬별 자동 자막·SNS 게시글·해시태그) + 씬↔실제 사진 매칭 + TTS 보이스 선택
- 완전 합성 영상이 아니라 **스크립트 + 농가 실제 사진 + 자동 자막 + TTS**를 템플릿으로 조립(`Content.assemblyMode: 'template'`). "서영왔서영"이 강조하는 진짜 산지 신뢰성과 합성 영상이 충돌하는 문제를 피하기 위한 설계.
- 실서비스 전환: `AIProvider` 인터페이스만 동일하게 구현해 `aiProvider`를 교체하면 나머지 코드 수정 없이 실제 영상 편집 API(Shotstack/Creatomate 류)·TTS API로 전환.

**(2) 완전 생성형 — OpenRouter 비디오 API** (`src/lib/openrouter.ts`, `/studio`·`/admin/studio`·`/studio/video`)
- `POST /api/v1/videos` (제출) → `GET /api/v1/videos/{id}` (폴링) → `GET .../content` (mp4 다운로드) / `GET /api/v1/videos/models` (모델·가격)
- 입력: 텍스트 프롬프트 + 이미지(시작/끝 프레임, base64 data URI). 모델은 응답의 `pricing_skus`로 단가 표시.
- 결과: Blob → `<video>` 재생 + 다운로드 + IndexedDB 영구 보관 + 상세페이지 첨부

**AI 보안·안정성**
- OpenRouter API 키는 사용자가 브라우저에서 직접 입력, **이 브라우저 전용 AES-GCM 키(추출 불가, IndexedDB)로 암호화**해 `localStorage`에 저장. 평문 키는 디스크에 남지 않고 같은 브라우저에서 자동 복호화. 전송은 HTTPS로 OpenRouter에만.
- 헤더 값 ASCII 제약 대응(키 비ASCII 문자 자동 제거), CORS/네트워크 차단 시 안내 메시지.
- 생성 실패·시간 초과·모델 안전정책 거부(예: 실존 인물 이미지) 시 사용자에게 설명 노출.

---

## ⑨ 수익모델(BM) — 5가지

> 모든 금액·요율은 **예시(임시)**. `src/lib/billing.ts` 한 곳에서 관리하며 화면에 "예시" 표기.

| # | 수익모델 | 방식 | 정책값(예시) |
|---|---|---|---|
| ① | **AI 콘텐츠 예치금(코인)** | 구독제 폐지. 코인 선충전 → 제작 시 차감. 큰 금액 충전 시 보너스 코인. | 1코인=100원, 숏폼 영상 1건 290코인, 촬영 대행 1,500 / 프리미엄 편집 500 / 추가 콘텐츠 190코인. 충전 팩 3만원→300 · 10만원→1,000+보너스120 · 30만원→3,000+보너스500. 가입 축하 100코인. |
| ② | 사이트 내 자체 판매 수수료 | 농가가 스토어에서 직접 판매, 판매액의 일정 %를 플랫폼 수수료로 | 판매액 8% (범위 5~15%), 배송비 3,500원 |
| ③ | 공동구매·라이브커머스 수수료 | 목표 수량 공동구매·라이브 판매액의 % | 판매액 8% (범위 5~15%) |
| ④ | 농산물 정기구독 (B2C) | 소비자가 농가 제철 박스를 정기 수령, 구독액의 % | 박스 월 35,000 / 2주 22,000원, 수수료 10% |
| ⑤ | 건별 부가서비스 | 현장 촬영 대행·프리미엄 편집 등 (코인 차감) | ①의 코인 단가 표 참조 |
| (참고) | 유통 소싱 수수료 (B2B) | 산지↔온라인 셀러 도매 중개, 거래액의 % | 거래액 5% (범위 3~7%) |
| (참고) | PB·브랜드 상품 | 자체 브랜드 마진 | 마진 15~35% |

- **"농가의 AI 콘텐츠 예치금(코인)"** 과 **"소비자의 농산물 정기구독"** 은 완전히 별개다.
- 관리자 매출은 **소싱 수수료 + 코인 충전액 + 자체 판매 수수료 + 공동구매 수수료 + 정기구독 수수료** 5줄로 집계. 미사용 코인 잔액은 "부채"로 별도 표시.

---

## ⑩ 운영·보안 전제

- 농가별 입력 데이터는 다른 농가에게 노출되지 않도록 API 레이어에서 권한 분리
- 콘텐츠·상세페이지 생성에 쓰이는 사진·정보는 해당 농가 동의 범위 안에서만 사용
- OpenRouter API 키·비밀값은 서버로 전송하지 않으며, 브라우저에 저장 시 암호화(§8 AI 보안). 실서버 전환 시 API 키는 서버 환경변수에만
- AI 생성 실패/무응답 시 농가·운영자용 안내 문구 마련(구현됨)
- `/admin`은 화면뿐 아니라 실서버 전환 시 서버·DB 권한에서도 운영자만 접근
- 영상 생성 모델의 콘텐츠 안전정책(실존 인물 이미지 거부 등)은 우회하지 않으며, 사용자에게 사유와 대안을 안내

---

## ⑪ 기술 스택 & 실행

- **프론트엔드**: Vite 5 + React 18 + TypeScript(strict) + react-router-dom 6. CSS 프레임워크 없이 디자인 시스템 1파일(`src/index.css`, `--brand-*`/`--amber-*` 토큰).
- **상태**: `localStorage` 목 DB(`src/lib/db.ts`), 유일한 인터페이스는 `src/lib/api.ts`. 영상 바이너리는 IndexedDB(`src/lib/videoStore.ts`).
- **AI**: `src/lib/ai.ts`(교체 가능한 `AIProvider`), `src/lib/openrouter.ts`(실제 비디오 API), `src/lib/secureKey.ts`(키 암호화).

```bash
npm install
npm run dev        # http://localhost:3047
npm run build      # tsc --noEmit + vite build

npx tsx scripts/smoke.tsx   # 전 페이지 SSR 렌더 (36 통과)
npx tsx scripts/mount.tsx   # 실제 클라이언트 마운트 + 콘솔 에러 0 (41/41 라우트)
npx tsx scripts/flow.tsx    # api.ts 시나리오 — 권한 분리 + BM 5종 + 코인 차감
```

- 데모 계정: 농가 `sunfarm@example.com` / `test1234` · 구매자 `buyer@example.com` / `test1234` · 운영자 `admin@youngfarm.ai` / `admin1234`
- 하단 "데모 데이터 초기화"로 언제든 처음 상태로 되돌림

---

## ⑫ v1 초기본 대비 변경 이력

| 항목 | 초기 기획(PRD_v1) | 최종본 |
|---|---|---|
| 범위 | 아산 지역 중심 | **전국 농산물·전국 농가** |
| 서비스 성격 | 콘텐츠 제작·유통 플랫폼 | **콘텐츠 + 커머스** (판매·공동구매·정기구독·소싱까지) |
| 페르소나 | 농가·운영자 2종 | **농가·구매자·운영자 3종** |
| 콘텐츠 흐름 | 데이터 → AI 영상 | 데이터 → **AI 상세페이지 확정** → AI 숏폼 스크립트 → 템플릿 조립 → 검수 |
| 영상 생성 | 개념만 | **템플릿 조립**(데모) + **OpenRouter 완전 생성형 스튜디오**(BYOK, 실제 mp4) |
| 과금 | 월 구독제(무료체험/기본형/고급형) | **예치금(코인) 선충전제로 전면 교체** — 구독·약정 없음, 충전 보너스 |
| 수익모델 | AI 콘텐츠 구독 중심 | **5개 BM** (①코인 ②자체판매 ③공동구매 ④정기구독 ⑤부가서비스) + 소싱·PB |
| 브랜드 | 없음 | 산지 방문 오리지널 시리즈 **〈서영왔서영〉** |
| 입력 폼 | 최소 세트 | **6단계 구조화 폼** (등급·인증·당도·수확일·가격대·배송·보관법 등) |
| UI | 핸드폰 목업 위주 | **웹=데스크톱 / 앱=모바일** 자동 전환, 가을 팔레트 |
| 열린 질문(§10) | 영상 길이·구독료·저작권·필드·검수시간 미정 | 영상 길이=15/30초 임시 옵션 유지 · 구독료→코인 단가로 대체 · 저작권=약관 안내 · 필드=6단계 확정 · 검수=상태(중/완료)로만 표기 |

---

## ⑬ 다음 단계 (v2 후보)

- 실제 백엔드/DB 연결 (`api.ts` 내부만 fetch로 교체)
- OpenRouter 호출용 얇은 프록시 + 팀 단위 키 관리
- 템플릿 조립 영상의 실제 렌더링 파이프라인(편집 API + TTS) 연동
- 농가 SNS 채널 연동·예약 게시
- 성과 학습: 조회수·클릭률·전환율을 AI 생성에 피드백
- 운영자 다계정·역할별 권한
