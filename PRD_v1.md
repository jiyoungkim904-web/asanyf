# PRD v1 — 영팜마켓AI (농가 데이터 → AI 콘텐츠 제작·제공)

## ① 한 줄 정의
영팜마켓AI는 전국 농가들의 농산물 데이터를 입력받아 생성형 AI로 SNS·판매 페이지용 영상 콘텐츠를 제작하고, 구독한 농가가 그 콘텐츠를 2차 활용할 수 있도록 제공하는 콘텐츠 제작·유통 플랫폼이다.

## ② 문제 — 지금 무엇이 불편한가
- 지역 농가들은 SNS나 판매 페이지에 올릴 홍보 영상을 직접 만들 시간·인력·노하우가 부족하다.
- 농산물 정보(품종, 재배방식, 수확시기, 특징 등)가 농가마다 흩어진 형태로만 있어, 콘텐츠 제작에 바로 쓸 수 있는 데이터로 정리되어 있지 않다.
- 영상 제작을 외주로 맡기면 비용이 크고, 매번 새 콘텐츠를 만들기 어렵다.

## ③ 사용자와 상황
- **구독 농가(사장님)**: 본인 농산물 정보를 직접 입력하고, 그 정보를 바탕으로 만들어진 영상 콘텐츠를 받아 자신의 SNS·판매 페이지에 게시하고 싶다.
- **가입 전 농가**: 서비스가 실제로 어떤 콘텐츠를 만들어주는지 확인하고 구독 여부를 결정하려 한다.
- **운영자(영팜마켓AI 팀)**: 여러 농가의 입력 데이터를 확인하고, AI 콘텐츠 생성을 실행·검수한 뒤 농가별로 전달한다.

## ④ 목표와 성공 기준
- 농가가 스스로 농산물 데이터를 입력할 수 있다 → 데이터 입력을 완료한 농가 수, 필수 항목 입력 완료율
- 입력된 데이터를 바탕으로 AI가 영상 콘텐츠를 생성할 수 있다 → 농가당 월평균 콘텐츠 생성 건수
- 구독 농가가 콘텐츠를 받아 2차 활용(SNS 게시 등)한다 → 콘텐츠 다운로드 비율, 다음 달 구독 갱신율

## ⑤ v1 범위 — 이번 버전에서 만드는 것
- 농가 회원가입/로그인 및 농가별 프로필
- **농산물 데이터 입력 폼**: 품목명, 품종, 재배방식(유기농/관행 등), 수확시기, 농가 스토리·특징, 사진 업로드 등 구체적 항목을 농가가 직접 입력
- 입력된 데이터를 기반으로 **AI 영상 콘텐츠 생성 요청** 기능
- 생성된 콘텐츠를 확인하고 **다운로드해 2차 사용**(SNS·판매 페이지 게시)할 수 있는 기능
- 관리자(/admin) 화면에서 농가별 입력 데이터, 생성 요청, 콘텐츠 현황을 확인하고 처리

## ⑥ 범위 밖 — v1에서 만들지 않을 것(이유 포함)
- **사람 검수 없는 완전 자동 생성·게시** — 콘텐츠 품질이 아직 검증되지 않아 사람이 확인한 뒤 전달한다
- **농가 SNS 계정 자동 연동·자동 게시** — 계정 연동과 권한 문제가 있어, v1은 다운로드까지만 제공하고 게시는 농가가 직접 한다
- **영상 직접 편집 기능(농가가 세부 수정)** — 편집기 구현 난이도가 높아 v2에서 검토한다
- **콘텐츠 저작권·2차 재판매 정책의 자동화** — 초기에는 약관과 수동 관리로 충분하다
- **영상 외 다른 콘텐츠 포맷(이미지, 카드뉴스 등)** — v1은 영상 콘텐츠 하나만으로 수요를 검증한다

## ⑦ 화면과 흐름
- **농가**: 회원가입 → 농산물 데이터 입력 폼 작성 → 콘텐츠 생성 요청 → 생성된 콘텐츠 확인/다운로드 → 본인 SNS·판매 페이지에 게시(농가가 직접)
- **운영자**: 로그인 → /admin에서 농가별 입력 데이터 확인 → AI 콘텐츠 생성 실행 및 검수 → 농가에게 콘텐츠 전달(발행)

## ⑧ 데이터와 AI
- **데이터**: `farms`(농가 정보), `farm_products`(농산물 데이터 — 품목·재배방식·수확시기·스토리·사진), `content_requests`(콘텐츠 생성 요청), `contents`(생성된 영상 콘텐츠와 처리 상태)
- **AI**: 농산물 데이터를 입력으로 받아 영상 콘텐츠(스크립트+비주얼) 초안을 생성한다. AI는 초안만 만들고, 운영자가 검수한 뒤 농가에게 발행 여부를 최종 결정한다.

## ⑨ 운영·보안 전제
- 농가별 입력 데이터는 다른 농가에게 노출되지 않도록 권한을 분리한다
- 콘텐츠 생성에 쓰이는 사진·정보는 해당 농가의 동의 범위 안에서만 사용한다
- API 키와 비밀값은 서버 환경 변수에만 저장하고 커밋하지 않는다
- AI 생성이 실패하거나 응답하지 않을 때 농가·운영자에게 보여줄 안내를 마련한다
- /admin은 화면뿐 아니라 서버·DB 권한에서도 관리자만 접근하게 한다

## ⑩ 열린 질문 — 아직 결정하지 못한 것
- 영상 콘텐츠는 어떤 포맷·길이(숏폼 15초? 30초?)로 만들 것인가?
- 구독료는 얼마이며, 월 콘텐츠 생성 횟수에 제한을 둘 것인가?
- 생성된 콘텐츠의 소유권과 2차 사용 범위(재판매 가능 여부 등)는 어떻게 정할 것인가?
- 농산물 데이터 입력 항목은 정확히 몇 개, 어떤 필드로 구성할 것인가?
- 콘텐츠 검수는 누가, 얼마나 걸릴 것으로 예상하는가?
AI 상담사를 넣고싶어